name: Build
on: [push, pull_request]
jobs:
  build:
    strategy:
      matrix:
        buildPlatform: [win,linux,mac]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 4
      uses: actions/setup-java@v2
      with:
        java-version: '14'
        distribution: 'adopt'
    - name: Build with Maven
      run: mvn -B package -Djavafx.platform=${{ matrix.buildPlatform }}
    - name: Upload JAR
      uses: actions/upload-artifact@v2.2.3
      with:
        name: yagpl-${{ matrix.buildPlatform }}.jar
        path: target/yagpl-jar-with-dependencies.jar
